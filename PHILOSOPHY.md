# God Simulation 项目哲学

> 状态：项目意图与哲学的共识文档。
>
> 本文蒸馏自 `VISION.md`（产品意图）与 `PROJECT_DIRECTION.md`（工程方向），回答"这个项目相信什么"。冲突时仍以 `VISION.md`、`PROJECT_DIRECTION.md` 原文为准，本文不改变其优先级，只做共识表达。

## 一句话定义

God Simulation（AI Pixel Sim）是一个**可暂停、可干预、可回溯、可分支的 AI 社会模拟沙盒**。玩家不是操作者，而是导演和剪辑师：面对的是一条由自主角色的认知、确定性物理和动态中断共同生成的社会时间线。

故事不来自预写剧情，来自三方共同作用：

```text
角色想做什么
  x 世界实际上允许什么
  x 玩家在何时、以什么方式介入
```

## 三方权力边界（不可越权）

| 角色 | 权力 | 禁区 |
| --- | --- | --- |
| Agent（LLM） | 基于有限认知产生**意图** | 不能读全局真相、不能宣布行动成功、不能改世界 |
| 世界（程序） | 裁决**事实**：寻路、碰撞、门、占用、时间 | 不替角色编写剧情结果 |
| 玩家（导演） | **剪辑**：放行时间、干预世界/意图/记忆、回滚分支 | 干预必须成为可见、可追溯的事件 |

推论：**物理即真理，意图不等于结果。** LLM 说"我打开了门"不算数，门必须由世界规则真正打开。禁止让 LLM 同时承担意图生成、规则判定和世界写入。

## 导演式中断循环

```text
冻结世界并取得一致快照
  -> 需要决策的角色异步思考（世界绝不偷偷推进）
  -> 玩家看懂局势，按空格放行
  -> 多个角色并发执行，物理逐步裁决
  -> 完成、冲突或前提崩塌 -> 全局立即熔断
  -> 事件成为相关角色的感知与记忆
  -> 只让受影响角色重思考，其他角色保留进度
  -> 玩家再次放行 / 干预 / 回滚开新分支
```

这不是回合制，也不是失控的实时模拟，而是由 AI 决策边界和世界事件切分节拍的**暂停式并发模拟**。

### 为什么必须全局冻结

- 所有角色共享同一条可解释的时间线，模型延迟不能让角色活在不同世界时刻。
- 玩家不会错过决定故事走向的认知节点。
- 一个角色造成的环境变化能在其他角色继续行动前被及时感知。
- 未受影响角色的执行进度可以保存和恢复，不陪同重新生成计划。
- 每次暂停天然形成一个可检查、可干预、可写入时间轴的"剪辑点"。

模型思考可以异步，**世界状态的推进必须同步、可重放**。

### 熔断是物理通向认知的桥

熔断不是报错处理，而是核心认知反馈机制：门被锁、目标被占用、路径崩塌时，角色应当在失败发生的那个世界时刻**立即"意识到现实变了"**——停止当前动作、生成具体感官反馈、写入记忆、触发全局暂停、局部重规划。失败不能表现为角色原地发呆，也不能事后才得知。

### 有限认知即故事发生器

角色只知道他实际看见、听见、经历过的事。数据库里门已是 LOCKED，没看见的角色就是不知道。客观事实、角色认知、玩家干预三者之间的**差异本身就是可产生故事的机制**——导演编辑记忆，改变的是角色相信的世界。

## 工程红线（哲学如何落成代码）

1. **唯一权威世界**：只有独立模拟进程持有可写 `WorldState`；浏览器只读、主程序只转发、数据库只记录、插件只提议效果、模型只选意图。"运行时世界才是当前事实，数据库是事实的持久化记录。"
2. **确定性**：固定 tick 是唯一可写时钟（`1 tick = 6 游戏秒`），种子随机，同输入必同结果。模拟 tick 绝不绑定渲染帧率。
3. **先检查点，后等待**：重要状态变化先形成原子检查点（Event + Snapshot 同事务提交），再开始不可控的外部等待（模型调用、异步检索）。
4. **事件优先**：重要变化先表达为带稳定 ID 的领域事件，再应用状态。角色知识和记忆必须引用真实 Event ID——"角色为什么知道这件事"永远可追溯。
5. **插件只认能力，不认类别**：核心不问"这是门还是冰箱"，只问"挡不挡路、能否自动通行"。插件返回 `EffectProposal`，由统一裁决层校验后一次性提交。
6. **不用 LLM 补世界规则**：规则缺失就补规则，权限缺失就补校验，不拿 Prompt 填工程缺口。
7. **错误明确暴露**：模型失败、保存失败、协议不匹配都显式阻塞并可重试，禁止静默兜底、禁止伪造成功。
8. **文档优先**：`VISION.md` > `PROJECT_DIRECTION.md` > ADR > 规格 > 测试 > 代码（前两份为 `private/` 下的本地私有文档，不入库）。实现服务于以上约束，不反向定义产品。

## 可玩性判据

这款游戏不需要先发明胜负条件。第一层可玩性来自一套完整、可理解、可反复操作的导演循环：看懂角色在想什么 -> 决定是否放行 -> 观察意图碰撞 -> 在暂停点检查因果 -> 修改世界、意图或记忆 -> 继续时间 -> 必要时回滚并比较分支。

判断项目走在正确方向上的事实标准：世界在冻结时绝不偷偷推进；模型调用不能直接改变事实；一名角色改变现实后另一名角色能即时感知、记住并重规划；玩家干预会作为事件进入当前分支；回到过去继续时创建新分支而不覆盖原历史；同一个机制场景可以用 Mock 确定性重放，也可以替换为真实模型运行。
