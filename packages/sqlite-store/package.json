{
  "name": "@god-sim/sqlite-store",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean --external better-sqlite3",
    "test": "vitest run packages/sqlite-store/src",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@god-sim/protocol": "workspace:*",
    "@god-sim/timeline": "workspace:*",
    "better-sqlite3": "13.0.3",
    "kysely": "0.29.5"
  },
  "devDependencies": {
    "@types/better-sqlite3": "9.6.0"
  }
}

